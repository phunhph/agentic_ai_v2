# Phase 7 Test: PlanningAgent Layer

## Mục tiêu
Kiểm tra khả năng lập lộ trình công việc (Todo-list) cho ExecutionAgent.

## Các bước thực hiện
1. **Gửi câu hỏi đa bước**.
2. **Kiểm tra AgentState**: (Xem qua log hoặc debug) Xem danh sách `plan` có chứa các task với thứ tự hợp lý không.

## Kết quả mong đợi
- [ ] Danh sách task được tạo ra với hành động cụ thể (execute_sql, format_data).
- [ ] Các task có sự phụ thuộc (input_from_task) đúng đắn.
